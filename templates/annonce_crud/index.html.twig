{% extends 'base.html.twig' %}

{% block title %}Annonces index{% endblock %}

{% block body %}
    <h1>Annonces index</h1>

    <table class="table">
        <thead>
            <tr>
                {# <th>Id</th> #}
                <th>Description</th>
                <th>Image</th>
                <th>Quantité</th>
                <th>adresse</th>
                <th>Date de publication</th>
                <th>Categorie</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for annonce in annonces %}
            <tr>
                {# <td>{{ annonce.id }}</td> #}
                <td>{{ annonce.Description }}</td>
                <td><img src="/uploads/{{ annonce.image }}" alt="Pas d'image" width="100" height="100"></td>
                <td>{{ annonce.Quantite }}</td>
                <td>{{ annonce.adresse }}</td>
                <td>{{ annonce.DatePublication }}</td>
                <td>{{ annonce.categorie.nom }}</td>
                <td>
                    <a href="{{ path('app_annonce_crud_show', {'id': annonce.id}) }}">show</a>
                    <a href="{{ path('app_annonce_crud_edit', {'id': annonce.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_annonce_crud_new') }}">Create new</a>
    <a href="/categorie">Create Categorie</a>
{% endblock %}
