{% extends 'base_back.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table table-striped">
        <tbody>
           
            <tr>
                <th scope="col">Nom_categ_event</th>
                <td>{{ categorieEvent.nomCategEvent }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorie_index') }}"><input type="button" class="btn btn-outline-success" value="retour"</a>

    <a href="{{ path('app_categorie_edit', {'id': categorieEvent.id}) }}"><input type="button" class="btn btn-outline-success" value="modifier"</a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
