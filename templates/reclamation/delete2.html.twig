<form method="post" action="{{ path('delete', {'id': reclamation.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reclamation.id) }}">
    <button href="{{path('app_reclamation_index')}}" class="btn btn-outline-danger">Supprimer</button>
</form>
