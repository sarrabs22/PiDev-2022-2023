{% extends 'base_back.html.twig' %}

{% block title %}CategoryD
{% endblock %}

{% block body %}
	<h1>CategoryD</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ category_d.id }}</td>
			</tr>
			<tr>
				<th>NameCa</th>
				<td>{{ category_d.NameCa }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_category_d_index') }}">
		<button type="button" class="btn btn-outline-success">Back To List</button>
	</a>

	<a href="{{ path('app_category_d_edit', {'id': category_d.id}) }}">
		<button type="button" class="btn btn-outline-success">Edit</button>
	</a>

	{{ include('category_d/_delete_form.html.twig') }}
{% endblock %}
