{# {% extends 'base.html.twig' %} #}
{% extends 'base_back.html.twig' %}

{% block title %}CategorieRec{% endblock %}

{% block body %}
    <h1>CategorieRec</h1>

    <table class="table">
        <tbody>
           
            <tr>
                <th>Nom</th>
                <td>{{ categorie_rec.Nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorie_rec_index') }}">Retour</a>

    <a href="{{ path('app_categorie_rec_edit', {'id': categorie_rec.id}) }}">Modifier</a>

    {{ include('categorie_rec/_delete_form.html.twig') }}
{% endblock %}
