<form method="post" action="{{ path('app_categorie_rec_delete', {'id': categorie_rec.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_rec.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
