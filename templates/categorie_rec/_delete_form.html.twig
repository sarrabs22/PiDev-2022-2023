<form method="post" action="{{ path('app_categorie_rec_delete', {'id': categorie_rec.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_rec.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
