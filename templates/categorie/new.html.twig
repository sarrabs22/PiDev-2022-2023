{% extends 'base_back.html.twig' %}

{% block title %}Ajour=t Categorie{% endblock %}

{% block body %}
    <h1>Saisir le nom de la categorie</h1>

    {{ include('categorie/_form.html.twig') }}
    {% if erreur == 1 %}
		<div class="alert alert-danger">{{ "Cette catégorie existe déja" }}</div>
	{% endif %}

    <a href="{{ path('app_categorie_index') }}">Retour</a>
{% endblock %}
